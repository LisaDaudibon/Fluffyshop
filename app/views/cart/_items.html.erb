<div class="container pt-5">
  <div class="row">
	  <div class="col-md-12">
       <h1>Panier (<%= current_user.cart.items.length %>)</h1>
	        <% if @current_user.cart.items.length < 1 %>
		      <p>Ton panier est vide.</p>
	        
          <% else %>
            <table class="table table-striped">
              <thead>
                <tr>
                  <th>Produit</th>
                  <th>Prix</th>
                  <th></th>
                </tr>
              </thead>

              <tbody>
                <% current_user.cart_items.each do |cart_item| %>
                  <tr>
                    <td><%= cart_item.item.name %></td>
                    <td><%= cart_item.item.price %>0€ </td>
                    <td><%= button_to "Supprimer", cart_item_path(cart_item), method: :delete, class: "btn btn-outline-danger" %></td>
                  </tr>
                <% end %>
              </tbody>
            </table>
            <p>
              <strong>Total: <%= @total %>€</strong>
              <%= render 'checkout' %>
            </p>
        <% end %>
      </div>
  </div>
</div>